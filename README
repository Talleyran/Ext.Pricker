pricker ['prɪkə]

сущ.

    1) инструмент для прокалывания, протыкания (игла, шило, дырокол)
    2) колючка; шип

Парсинг отчасти готов. Во время парсинга так же осуществляется приведение типов ( int, float, date ),
неплохо было бы указывать тип в шаблоне геосервера, т.к. сейчас я использую регуальрные выражения, чтобы определить формат данных.
Далее необходимо менять тип оси в соответствии с форматом данных,
Необходима функция, которая будет упорядочивать масив значений по X и вместе с ним упорядочивть все другие значения (иначе вы видите что происходит при числовых значения по обеим осям).
